# GiG-MDA 复现依赖
# 建议 Python 3.9+ / Anaconda 环境
# 安装: pip install -r requirements.txt

# 核心数据科学
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0

# 机器学习分类器
# 注: r2_config.py 中 XGB_CONFIG 的 device='cuda' 在无 GPU 时需改为 'cpu'
xgboost>=2.0.0
lightgbm>=4.0.0

# 深度模型 (GCN 评估, gcn_eval.py)
torch>=2.0.0

# 化学信息学 (ECFP32 预训练对照与 scaffold-disjoint 划分)
rdkit>=2023.3.1

# 可选: 分子预训练表示 (MoLFormer) 需要 transformers + 联网下载权重;
# 论文中的 MolEmb32 特征文件已随数据发布, 纯复现评估不需要重新提取
# transformers>=4.30.0
