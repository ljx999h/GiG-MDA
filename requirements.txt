# GiG-MDA 依赖清单
# 建议使用 Python 3.9+ / Anaconda 环境
# 安装: pip install -r requirements.txt

# 核心数据科学
numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0

# 机器学习分类器 (XGBoost 需 GPU, 无 GPU 时去掉 device='cuda' 参数)
xgboost>=2.0.0
lightgbm>=4.0.0

# 一次性工具 (仅 insert_ablation.py 需要, 非论文复现必需)
reportlab>=4.0.0
pikepdf>=8.0.0

# 可选: torch (仅部分可视化脚本需要, 非必需)
# torch>=2.0.0
